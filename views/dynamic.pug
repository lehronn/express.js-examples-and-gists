doctype html
html
  head
    title= name
  body
    h1 Jade - node template engine
    p.greetings#people I`m rendered from pug!
    div.container.column.main#division(width="100",height="100")
    h3= name
    
    //interpolacja zmiennej, czyli mamy przekazanie zmiennej z logiki server.js do widoku dynamic.pug.
    h3 Pozdrowienia od #{name}
      br
      a(href= url) URL
      br
      a(href= url) URL
      
    //pug pozwala też na ify i pętle.
    if(user)
      h2 Hi, #{user.name}
    else
      a(href="/sign_up") Sign Up